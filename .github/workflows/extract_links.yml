name: Extract Download Links

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  extract-links:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install selenium requests webdriver-manager

    - name: Install Google Chrome
      run: |
        sudo apt-get update
        sudo apt-get install -y google-chrome-stable

    - name: Run Python script to extract links
      run: |
        # 这一步会返回 ChromeDriver 的安装路径，并将其设置为环境变量
        export CHROMEDRIVER_PATH=$(python -c "from webdriver_manager.chrome import ChromeDriverManager; print(ChromeDriverManager().install())")
        # 运行你的 Python 脚本
        python extract_radio_links.py

    - name: Upload links.txt artifact
      uses: actions/upload-artifact@v4
      with:
        name: extracted-links
        path: links.txt
        retention-days: 7

```

**确认你的文件结构：**

请确保你的 GitHub 仓库的根目录包含以下两个文件：

```
your-repo-name/
├── .github/
│   └── workflows/
│       └── extract_links.yml
└── extract_radio_links.py  # 你的 Python 脚本
